---
status: PASS (after fix-forward)
reviewer: "clean-context independent review (sdd:reviewer + coordinator cross-check)"
date: "2026-09-20"
---

# Review record — common

## Scope

Diff under review: `git diff b503c3e..HEAD` (base = `tasks: common` commit, the branch point off
`main`) — 3 commits on `feature/common-module`:

- `4530839` infra: rename envelope types; add array-shape check to core client (T1, AC-03)
- `cf3c828` feat(common): reference-list types, module factory, exports, and tests (T2-T5, AC-01/02/03/04/05/06/08)
- `5d90658` test(common): verify all 15 methods in both published build surfaces (T6, AC-07)

```
docs/features/common/tasks/tracker.md |  12 +--
src/client.ts                         |   8 +-
src/index.ts                          |  24 +++++-
src/modules/common/index.ts           |  60 ++++++++++++++
src/types/common.ts                   |  69 ++++++++++++----
src/types/envelope.ts                 |  15 ++++
test/unit/build-surface.test.ts       |  39 +++++++++
test/unit/client.test.ts              |  49 ++++++++++++
test/unit/modules/common.test.ts      | 147 ++++++++++++++++++++++++++++++++++
9 files changed, 401 insertions(+), 22 deletions(-)
```

## Traceability (stage 1)

US-01..US-06 each map to ≥1 AC and a `sad.md` §6 flow (US-05/US-06 justified non-runtime, per
published-`.d.ts` discoverability and the cross-module invariant respectively — no code path
needed). AC-01/02/03/06 genuinely reach code and non-tautological tests. AC-04/05 reach code but
were under-asserted (finding 3). **AC-08 reached neither working code nor a real test** (findings
1-2) — the most severe gap, now resolved below.

## Findings and resolutions

| # | Severity | Summary | file:line | AC/contract | Resolution |
|---|---|---|---|---|---|
| 1 | Blocker | `fetch()` rejection and `response.json()` parse failure propagate as raw native errors, not `NovaPoshtaApiError` | `src/client.ts:30`, `src/client.ts:40` | AC-08; `sad.md` §6 Flow 1; `contracts/public-api.md` §4 row 1; spec §6 NFR row 2 | **Fix now** |
| 2 | Blocker | The two AC-08 tests use bare `.rejects.toThrow()` with no error class, so they pass even when the wrong error type is thrown — the tautology that let finding 1 ship | `test/unit/modules/common.test.ts:102,115` | AC-08 test adequacy | **Fix now** |
| 3 | Major | `envelope.errors.join(...)` assumes the field is always present; a decline response omitting it crashes with a raw `TypeError` instead of `NovaPoshtaApiError` | `src/client.ts:44` | AC-04/AC-05; spec §1 Decision override (NP responses are inconsistent); §6 NFR row 2 | **Fix now** |
| 4 | Major | AC-04/AC-05 tests assert only that *some* `NovaPoshtaApiError` was thrown, never that Nova Poshta's own `errors`/`errorCodes` reached the caller | `test/unit/modules/common.test.ts:67-93` | AC-04, AC-05; `contracts/public-api.md` §4 | **Fix now** |
| 5 | Medium | AC-07 build-surface check is a raw substring match — `getTypesOfPayers` is a substring of `getTypesOfPayersForRedelivery`, so deleting a method can still pass | `test/unit/build-surface.test.ts:36` | AC-07 test adequacy | **Fix now** |
| 6 | Medium | 13 of 15 methods have no test asserting the `calledMethod` string sent to Nova Poshta; a copy-paste typo ships silently | `test/unit/modules/common.test.ts` | spec §6 NFR row 2 ("100% of in-scope methods"), QG-4 | **Fix now** |
| 7 | Low | Test title says "plain object" but fixture is `data: null` | `test/unit/client.test.ts:27-28` | test hygiene | **Fix now** |
| 8 | Low | `build-surface.test.ts` reads `dist/index.d.ts` relative to cwd; works in CI (build-before-test), fails standalone `npm test` on a clean checkout | `test/unit/build-surface.test.ts:29` | test robustness | **Fix now** (resolve path independent of cwd) |
| 9 | Low | `contracts/public-api.md` prose (§3 note) lists `AlternativePayerType`'s `Ref` as an `OpenEnum` candidate, but its own code block declares it as plain `ReferenceRecordBase`. Implementation matches the code block, not the prose — doc-only drift, not a code defect | `docs/features/common/contracts/public-api.md:155-156` vs `:136` | doc consistency | **Not an issue for code** — doc-only note, no code change |
| 10 | Info | `OpenEnum<never>` on `PaymentForm.Ref`/`PayerType.Ref`/`CounterpartyType.Ref` compiles as plain `string` (no autocomplete) because `Known` hasn't been populated from a live API call — this is the deliberate, contractually-specified deferral (`contracts/public-api.md` §2, `tasks/t2-reference-list-types.md`), not a defect. spec.md §8's two implement-gating open questions (filter-rejection behavior, live-value capture) remain unchecked | `src/types/common.ts:24,47,54` | ADR-0002 (matches); spec §8 open questions | **Acknowledged, no action** — already tracked in spec.md §8 |

No stage-1 (AC) finding is being shipped unresolved: findings 1-4 are the AC-08/AC-04/AC-05
gaps and are all fixed, not deferred. Findings 5-6 are stage-2 test-adequacy gaps guarding those
same ACs, also fixed. Findings 7-9 are cosmetic/doc-only. Finding 10 is not a defect — it is the
feature working exactly as its own contract and ADR-0002 specify, with the still-open spec §8
items already tracked with an owner.

## Fix-forward commits

Applied via `/sdd:implement common`, TDD per finding (RED confirmed against real code before the
fix — see commit bodies), staying in the same review context per the skill's no-`/clear` rule:

- `9a9d339` — fix(common): wrap fetch/JSON-parse failures as `NovaPoshtaApiError`, guard missing
  decline fields (findings 1, 3, 4, 7; `SDD-Task: T1, T5`; `SDD-AC: AC-04, AC-05, AC-08`)
- `534a295` — test(common): close AC-07 substring blind spot, cover all 15 `calledMethod` values
  (findings 5, 6, 8; `SDD-Task: T6`; `SDD-AC: AC-07`)

Full gate after both commits: `npm run build` clean, `npm test` 34/34 green (was 16), `npm run
lint` clean. Each RED was verified against the pre-fix code before the fix landed (5 genuine test
failures reproducing findings 1 and 3; a temporary typo injection reproducing finding 6's blind
spot; a substring proof-of-concept reproducing finding 5's blind spot) — no test was written
loose enough to pass without the underlying fix.

## Verdict

**PASS** — all stage-1 (AC) findings (1-4) and their guarding stage-2 test-adequacy findings
(5-6, 8) are fixed and gate-clean; findings 7 (test hygiene) is fixed; finding 9 is a doc-only
note; finding 10 is not a defect. Ready for `/sdd:ship common`.
