---
status: CHANGES REQUESTED
reviewer: sdd:reviewer (clean-context, independent)
date: "2026-09-21"
---

# Review — internet-document

## Scope

- Diff: `f04dd5f..94d3893` (`spec: internet-document` through `tracker: internet-document — all 9 tasks done`)
- 73 files changed, 8008 insertions(+), 28 deletions(-) (includes prior `address`/`counterparty` test scaffolding touched incidentally; internet-document's own surface: `src/modules/internet-document/index.ts` (148), `src/types/internet-document.ts` (213), `src/client.ts` (+5), `src/index.ts` (+88 total, ~30 internet-document-specific), `test/unit/modules/internet-document.test.ts` (511), `test/unit/build-surface.test.ts` (+38), README usage example)
- Commit trailers across the range claim AC-01 through AC-19 (all in-scope ACs)

## Stage 1 — AC / trace

All 12 user stories (US-01..US-12) trace to ≥1 AC and a §6 sequence flow; AC-02/13/17/18/19 are justified as non-runtime in `sad.md:383-395`. `data-model.md`'s no-schema-change outcome is coherent with the library's stateless design (CLAUDE.md).

Two AC-02 findings and one AC-08 finding are genuine stage-1 gaps — the claimed compile-time guard and the claimed per-Ref rejection reason are not actually implemented as specified. All three resolved **Fix now** (below).

Confirmed genuinely satisfied and non-vacuously tested: AC-05 (empty-on-success), AC-07 (per-Ref array, never collapsed/thrown for partial), AC-09/AC-10 (pass-through, no injected pagination), AC-14/15/16 (all 6 enveloped methods), AC-17/AC-18.

## Stage 2 — quality

Module/factory layout, `firstOrUndefined` duplication, and casts all match `src/modules/address/index.ts` and `src/modules/counterparty/index.ts` precedent. `NovaPoshtaApiError` reused unchanged. Eight quality findings below (test adequacy, a security regression, a doc hazard, a resource-handling gap).

## Findings

| # | Stage | Finding | Location | AC/contract | Verdict |
|---|---|---|---|---|---|
| 1 | 1 | `CargoTypeDetail` carries only `CargoType`; every cargo type (Pallet/Documents/Parcel/Cargo) requires byte-identical fields — no payload can fail to compile because of cargo type, contradicting ADR-0001's "~4–5 CargoType variants ... combined via intersection" | `src/types/internet-document.ts:71-75,109` | AC-02, ADR-0001, §6 NFR "Save/update discriminant guard" | **Fix now** |
| 2 | 1 | Sender address fields are fixed (`SenderAddress: string`) across all four delivery-method payloads — only the recipient leg is discriminated; a door-leg payload compiles with no sender street/house/flat | `src/types/internet-document.ts:37-60` | AC-02 | **Fix now** |
| 3 | 2 | `apiKey` became an enumerable own property on the client object — `JSON.stringify(client)` / logging now leaks the raw key, a regression introduced for this module's print path | `src/client.ts:23,29` | §6.1 credential handling, ADR-0003 driver | **Fix now** |
| 4 | 1 | AC-08's "Nova Poshta's own reason for each rejected [Ref]" is a hardcoded placeholder string on every path, never read from the response; the test only asserts "some string" so it can't catch this | `src/modules/internet-document/index.ts:121`; test at `test/unit/modules/internet-document.test.ts:165` | AC-08, ADR-0002 | **Fix now** |
| 5 | 2 | AC-02's type-level tests check one of four delivery-method legs, never the cargo-type axis, and live outside the repo's convention (`test/unit/types/<domain>.test.ts`, per `address`/`counterparty`) | `test/unit/modules/internet-document.test.ts:107-128` | AC-02, test-plan.md:21,34 | **Fix now** |
| 6 | 2 | Print-link tests assert `toContain(ref)`/`toContain(apiKey)` against a fetch stub that returns success for any URL — a wrong endpoint, path, or parameter order would still pass; `Type`/`Copies` and the empty-`Documents` rejection are uncovered | `test/unit/modules/internet-document.test.ts:259-299`; `src/modules/internet-document/index.ts:70-77` | AC-11/AC-12, contracts/public-api.md:338-342 | **Fix now** |
| 7 | 2 | README usage example non-null-asserts `waybill!.Ref` immediately after `save()`, which AC-05 documents can return `undefined` on success-with-no-record — the one example developers copy models the unsafe pattern AC-05 exists to prevent | `README.md:61` | AC-05, AC-17 | **Fix now** |
| 8 | 2 | AC-06's unit test asserts `BackwardDeliveryData` is absent from the serialized body when passed as `undefined` — true regardless of module behavior, since `JSON.stringify` drops `undefined` keys unconditionally; non-falsifiable | `test/unit/modules/internet-document.test.ts:97-105` | AC-06 | **Fix now** |
| 9 | 2 | test-plan.md marks 9 ACs as needing unit+integration coverage; no `test/integration/internet-document.test.ts` exists (unlike `counterparty`'s equivalent), and the tracker marks the epic done regardless | `docs/features/internet-document/test-plan.md:33-44,66-67` | AC-01/03/04/06/07/09/10/11/12 | **Fix now** |
| 10 | 2 | AC-19's build-surface check text-matches method names in the built `.d.ts`/`.d.cts` files rather than importing and type-checking the built output — a wrongly-typed method would still pass. Pre-existing gap shared by `address`/`counterparty`'s equivalent checks | `test/unit/build-surface.test.ts:167-192` | AC-19, test-plan.md:51 | **Deferred** — spec.md §8 (cross-module fix, owner: Tech Lead) |
| 11 | 2 | Print-link verification `fetch()`s the URL but never reads/cancels the response body — can hold the connection open until GC against a real endpoint, once per print call | `src/modules/internet-document/index.ts:79-86` | §6 NFR (overhead), general resource handling | **Fix now** |

Not flagged, per reviewer instruction: `spec.md` §8 OQ-1..OQ-4 (exact print wire shape, delivery×cargo cardinality, success-path warnings/`totalCount`, print-link wrapper type) — already tracked as open.

## Resolution

- 10 findings → **Fix now**: handed back to `implement` as follow-up work on the same feature branch (no `/clear` — see handoff).
- 1 finding (#10, AC-19 check shallowness) → **Deferred**, recorded in `spec.md` §8 with owner (Tech Lead) and due date (before the next module's AC-19-equivalent check is written), since it's a repo-wide pattern shared by two already-shipped sibling modules and shouldn't be fixed inconsistently for one module alone.

## Verdict

**CHANGES REQUESTED**
