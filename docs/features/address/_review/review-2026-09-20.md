---
status: PASS
reviewer: "independent clean-context reviewer (sdd:reviewer, opus, xhigh)"
date: "2026-09-20"
---

# Review record — address

## Scope

Diff: `main..HEAD` on `feature/address-module` (commits `08e8b1f`..`4b8e536`, 11 commits).

```
 README.md                              |  10 +-
 docs/features/address/tasks/tracker.md |  20 +-
 src/index.ts                           |  26 +++
 src/modules/address/index.ts           |  91 ++++++++
 src/types/address.ts                   | 138 ++++++++++++
 test/unit/build-surface.test.ts        |  36 ++++
 test/unit/modules/address.test.ts      | 370 +++++++++++++++++++++++++++++++++
 test/unit/smoke.test.ts                |   7 +-
 test/unit/types/address.test.ts        |  34 +++
 9 files changed, 716 insertions(+), 16 deletions(-)
```

Baseline read: `spec.md` (§4 US-01..US-10, §5 AC-01..AC-13, §6 NFRs, Test plan), `sad.md` (§5, §6
runtime flows + coverage-check table, ADR-0001), `data-model.md` (legitimate no-schema-change),
`contracts/public-api.md` + `api-sync-report.md`, `tasks.json` (T1-T10), `adr/0001-...md`.

## Stage 1 — AC/US trace

All 13 ACs (AC-01..AC-13) and all 10 user stories (US-01..US-10) were traced end-to-end
(spec → sad §6 → tasks.json → implementation → test). `npx tsc --noEmit`, `npm test` (64 tests
pre-fix), `npm run lint`, and `npm run build` all passed before any review fix was applied.

Confirmed genuinely satisfied prior to fixes: AC-01, AC-02, AC-03, AC-04, AC-06, AC-07, AC-08,
AC-09, AC-10. No user story or acceptance criterion was entirely missing from the diff.

## Findings

| # | AC / US | Finding | Verdict | Outcome |
|---|---|---|---|---|
| 1 | AC-05 / §6 NFR row 3 | No `tsc --noEmit` step anywhere in CI — the compile-time full-replace guard (and its `@ts-expect-error` tests) could regress without CI catching it. Reproduced: loosening `UpdateAddressPayload` to `Partial<...>` left `build`/`test`/`lint` all green; only `tsc --noEmit` caught it. | CONFIRMED | **Fixed** — added `"typecheck": "tsc --noEmit"` to `package.json` scripts and a `npm run typecheck` CI step in `.github/workflows/ci.yml`, before `npm test`. Re-verified the same regression now fails typecheck (`TS2578`). |
| 2 | AC-13 / `contracts/public-api.md` §7 | The published-build type-surface test read `dist/index.d.ts`/`dist/index.d.cts` off disk directly instead of resolving through `package.json` `exports`, and `exports["."]` had no `require.types` condition — a real CJS consumer under `moduleResolution: node16` could not resolve the package's types at all (confirmed: `TS1479`), yet the check passed. Pre-existing from `common`, extended (not introduced) by this feature's new `AC-13` test block. | CONFIRMED | **Fixed** — split `package.json` `exports["."]` into `import`/`require` conditions each with its own `types`; rewrote `test/unit/build-surface.test.ts` to resolve the ESM/CJS declaration paths through the `exports` field itself (throws a clear error if either condition's `types` is missing), so a regression to the old flat shape now fails the test. Re-verified a real CJS consumer resolves cleanly (`tsc --noEmit`, exit 0). |
| 3 | AC-12 / US-10 | The only AC-12-tagged test (`test/unit/smoke.test.ts`) only asserted the factory doesn't throw — nothing would fail if a future edit added memoization/caching to a lookup. | CONFIRMED | **Fixed** — added a test in `test/unit/modules/address.test.ts` ("address module — no caching across calls (AC-12)") calling `getCities` twice with different filters against a stateful mock, asserting two independent `fetch` calls with two distinct, non-stale results. |
| 4 | AC-11 / `contracts/public-api.md` §6 | `findCityByName` returned `City \| undefined` (`cities[0]`), silently discarding every match after the first — itself a post-call reshaping of the response, which AC-11 and the contract's §6 convenience-method clause forbid ("no new response type invented... no post-call re-filtering, sorting, or reshaping"). `FindByString` is a substring match and can legitimately return more than one city. | CONFIRMED | **Fixed** — changed `findCityByName` to return `Promise<City[]>` (the same shape `getCities` returns for the equivalent input), matching the contract literally. Updated `AddressModule` interface, implementation, tests, and `tasks/t6-find-city-by-name.md`. |

### Notes (not blockers, not acted on)

- `test/unit/build-surface.test.ts`'s outer `describe("published build surface (AC-07)")` label
  predates this feature (inherited from `common`) and is stale/unrelated to AC-07's actual meaning
  in this spec — out of scope for this diff, left as-is.
- The §6 NFR row 4 overhead benchmark is low-signal with `fetch` stubbed in-process (can only catch
  a gross regression) but is not flaky; left as shipped.
- `spec.md` Test plan marks AC-01/AC-02 as `unit + integration`; `test/integration/` still only
  holds `.gitkeep`. Never blocks CI (gated on `NOVA_POSHTA_TEST_API_KEY`); tracked as a pre-existing
  gap, not introduced by this feature.

## Stage 2 — quality

No findings. Module follows `common`'s factory shape exactly, zero `any` in public signatures,
`NovaPoshtaApiError` reused unchanged, no reach into `common` beyond the shared `OpenEnum` type,
`src/client.ts` untouched (per spec.md §1 Decision override), no client-side validation or caching
added (matches §3 non-goals), write payloads never logged.

## Verification after fixes

`npm run build`, `npm run typecheck`, `npm test` (66 tests), `npm run lint` all pass. Manually
verified a real CJS consumer (`moduleResolution: node16`) resolves `nova-poshta-lib`'s types
cleanly post-fix, and that reverting `UpdateAddressPayload`'s guard now fails `npm run typecheck`.

## Gate result: **PASS**

All four stage-1 findings were fixed in this review pass (not deferred, per the team's decision not
to leave any open). No open stage-1 finding remains.
