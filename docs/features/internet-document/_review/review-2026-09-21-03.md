---
status: CHANGES REQUESTED
reviewer: sdd:reviewer (clean-context, independent)
date: "2026-09-21"
supersedes: review-2026-09-21-02.md
---

# Review — internet-document (third pass)

## Scope

- Diff: `main..HEAD` (full feature diff, `main` at `53167cd`), re-verified against `review-2026-09-21-02.md`'s
  7 residual findings (N1–N7) plus a fresh end-to-end stage-1/stage-2 pass over the whole change.
- Commits since round 2: `c135041`, `67f204d`, `579d287`, `024da48`, `0b8438b`, `c992278`.
- Verified with `npm run typecheck` / `npm test` (185 passed, 2 integration skipped) / `npm run lint`, plus
  targeted mutation testing of the fixed assertions.

## Stage 0 — disposition of round 2's N1–N7

| # | Round-2 finding | Verdict |
|---|---|---|
| N1 | ADR-0004 doc propagation gap | **PARTIALLY RESOLVED** — `tasks.json`, `sad.md` §1/§2/§4/§9/§12, `api-sync-report.md:52` fixed; residual drift → P8 |
| N2 | Recipient-leg AC-02 axis untested | **RESOLVED** — mutation-proved negatives added |
| N3 | AC-08 reason attribution (dropped errors, one joined string) | **RESOLVED** in code; contract doc left stale → P2 |
| N4 | README `counterparty.save()` hazard | **RESOLVED** |
| N5 | Unconfirmed `HEAD` assumption on print verification | **RESOLVED** — switched to `GET`, discards body |
| N6 | `requestEnvelope()` boundary crossing undocumented | **PARTIALLY RESOLVED** — documented for `delete`; a second, still-undocumented shared-client change found → P3 |
| N7 | Missing `NovaPoshtaSuccessEnvelope` export | **RESOLVED** |

## Findings (this pass)

| # | Stage | Finding | Location | AC/contract | Resolution |
|---|---|---|---|---|---|
| P1 | 1 | `delete` sends the JSON field `Documents`; this feature's own contract research (`api-sync-report.md:94-99`, `public-api.md:200-201`) documents the real wire field as `DocumentRefs` — nothing maps one to the other, and the unit test pins the wrong name too, so a real `delete` call would likely be rejected or no-op | `src/modules/internet-document/index.ts:117-121`; `src/types/internet-document.ts:142-144`; `test/unit/modules/internet-document.test.ts:546` | AC-07, AC-08 | **Fix now** |
| P2 | 2 | `contracts/public-api.md:213-214` still documents the pre-N3 behavior ("every rejected Ref in one batch shares the same joined text"), contradicting the actual per-Ref-match-with-fallback code N3 shipped | `contracts/public-api.md:213-214` | AC-08 | **Fix now** |
| P3 | 2 | A second, undocumented shared-client change: `src/client.ts` now exposes a public `readonly apiKey` member for the print-link path, while `spec.md:61`'s non-goal amendment and `adr/0003-...md` still say the print path stays entirely within this module's own files | `src/client.ts:29-33`; `spec.md:61`; `adr/0003-construct-then-verify-print-links.md:36-37` | §3 non-goal | **Fix now** |
| P4 | 2 | `sad.md:223` still lists `delete` as delegating to `client.request()` (it now uses `requestEnvelope()`); `sad.md:505`'s risk row still frames success-path warnings as unexposed, though `requestEnvelope()` now exposes them | `sad.md:223`; `sad.md:505` | design fidelity | **Fix now** |
| P5 | 2 | `void response.body?.cancel()` can reject if the stream is already errored; nothing catches it, risking an unhandled rejection from inside a library call | `src/modules/internet-document/index.ts:91` | n/a | **Fix now** |
| P6 | 2 | The round-1 body-discard fix has no falsifiable test — every print-link test stub omits `body`, so deleting the `cancel()` line leaves the suite green | `test/unit/modules/internet-document.test.ts:317-319,338-341,366-369` | n/a | **Fix now** |
| P7 | 2 | GET-and-discard verification only detects a non-2xx status, not the blank/error 200 page AC-11 names as a risk; ADR-0003/OQ-1 don't record this residual gap | `src/modules/internet-document/index.ts:84-95`; `adr/0003-...md`; `spec.md:288` | AC-11, AC-12, §8 OQ-1 | **Fix now** |
| P8 | 2 | Residual N1 drift: `sad.md:200,233`, `contracts/api-sync-report.md:125`, `tasks/_epic.md:69-70`, `tasks/t1-internet-document-domain-types.md:32,49,63` still describe the superseded two-axis design as current | `sad.md:200,233`; `api-sync-report.md:125`; `tasks/_epic.md:69-70`; `tasks/t1-...:32,49,63` | AC-02, ADR-0004 | **Fix now** |

Stage-1 end-to-end trace is otherwise clean: all 12 user stories reach ≥1 AC and a `sad.md` §6 flow; all 19
ACs reach code plus a non-tautological test; ADR-0002/0003/0004 match the shipped code; no `apiKey` leak
surface beyond the by-design AC-13 print link.

## Resolution

All 8 findings → **Fix now**, resolved by the user via `AskUserQuestion`. Handed back to `implement` as
follow-up work on the same feature branch (no `/clear`).

## Verdict

**CHANGES REQUESTED**
