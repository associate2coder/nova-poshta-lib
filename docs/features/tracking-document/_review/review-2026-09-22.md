---
status: PASS
reviewer: "independent clean-context reviewer (sdd:reviewer, opus, xhigh) + Tech Lead follow-up"
date: "2026-09-22"
---

# Review record — tracking-document

## Scope

Diff: `ef06f6d..HEAD` on `fix/internet-document-api-contract-gaps` (10 commits touching this
feature: `6a3df42` clarify .. `8a38047` tracker). Reviewed only the `tracking-document`-scoped
subset of the branch — `internet-document`-related changes on the same branch already have their
own closed review records (`docs/features/internet-document/_review/review-2026-09-22-08.md`,
PASS) and were out of scope here.

```
docs/features/tracking-document/**                        (spec, sad, adr, contracts, tasks)
src/modules/tracking-document/index.ts                     |  37 ++
src/types/tracking-document.ts                              | 164 ++++
src/index.ts                                                 |   9 +  (export block)
test/unit/modules/tracking-document.test.ts                 | 394 ++  (+41 in this pass, see below)
test/unit/types/tracking-document.test.ts                   | 193 ++
test/integration/tracking-document.test.ts                  |  28 ++
test/unit/build-surface.test.ts                              |  24 +-  (tracking-document block)
README.md                                                     |  27 +-  (usage snippet)
```

Baseline read: `spec.md` (§4 US-01..US-07, §5 AC-01..AC-11, §6 NFRs, §6.1 security, §8 open
questions, inline test plan), `sad.md` (§4 decisions 1-7 + ADR-0001, §6 both runtime flows),
`contracts/public-api.md` + `contracts/api-sync-report.md`, `tasks.json` (T1-T8), `adr/0001-...md`.

## Stage 1 — AC/US trace

All 7 user stories (US-01..US-07) have ≥1 AC and appear in `sad.md` §6's two flows. All 11
acceptance criteria (AC-01..AC-11) trace to both code and an assertion (not just an `it()`
description) in `test/unit/modules/tracking-document.test.ts` / `test/unit/types/tracking-document.test.ts`.
`npm test` (216 passed / 3 skipped before this pass's additions), `npm run build`, `npm run lint`
all green before any review fix.

Field-level check: `TrackingStatus` claims "118 fields" in `spec.md`, `sad.md`, both `contracts/`
files, and the shipped `src/types/tracking-document.ts`. Independently recounted (digit-tolerant
parser, since two field names — `PossibilityChangeCash2Card`, `PostomatV3CellReservationNumber` —
contain digits that a naive regex undercounts) — confirmed **118 in both** the implementation and
the contract, identical names and types, zero duplicates, zero `any`. The claim is accurate.

## Findings

| # | AC / clause | Finding | Verdict | Outcome |
|---|---|---|---|---|
| 1 | AC-09 / §6.1 | `spec.md` §8 had 3 open questions explicitly gated "due: before `sdd:implement tracking-document`" (API-key requirement, re-verify method surface against official docs, who performs the required §6.1 security review) — all still unchecked after implementation had already completed (8 commits, tracker.md all `done`). No `_review/` record existed yet, unlike `internet-document`. | CONFIRMED | **Fixed** — resolved all 3 in this pass (see below); `spec.md` §8 and §6.1, `sad.md` §2/§11 updated in place. |
| 2 | sourcing policy (CLAUDE.md) | `contracts/api-sync-report.md` typed 15 of 118 fields at only `medium` (single-sourced) confidence, and its stated reason for not re-fetching a third SDK (`sirkostya009/go-novapost` "doesn't declare the full response field set") was contradicted by `spec.md`'s own earlier text, which already quotes that SDK's `StatusDocument` struct. | CONFIRMED | **Fixed** — re-fetched `sirkostya009/go-novapost`'s `tracking_document.go` in full via `gh api`. It does declare a complete 107-field `StatusDocument` struct. Corroborates all 4 of the former TS-only fields (raised medium→high), the method surface, and the 21-value status-code list; doesn't corroborate the 11 Go-only fields (stay `medium`, now checked against 2 sources instead of 1). No field set change — union stays 118. `contracts/api-sync-report.md` rewritten with the full finding. |
| 3 | spec.md §8 OQ / sad.md §11 | `spec.md`/`sad.md` both stated "every response field is typed as optional" (the stated default for the still-open phone-masking question) — but the shipped `TrackingStatus` has zero optional fields, all 118 required. `contracts/api-sync-report.md`'s own "Open items" section had already flagged this same drift without it being back-fed into `spec.md`/`sad.md`. | CONFIRMED | **Fixed** — corrected both documents in place to state fields are required, and that a masked field (if Nova Poshta does mask on phone mismatch) would show up as an empty/zero value, not an absent property. Left the underlying phone-masking question itself open (genuinely unresolved, needs a live API key) — only the inaccurate "Default now" text was wrong. |
| 4 | AC-05 / §6 NFR row 3 | The one AC-05 test fixture (`test/unit/modules/tracking-document.test.ts:167`, pre-fix) only exercised a longer + reordered response, never one with **fewer** records than requested — the case `spec.md` §5 AC-05 and its NFR test-plan row name first ("dedicated fixture with a short/reordered response"). The zero-record success case (`spec.md` AC-08's own "smallest instance of AC-05" note) was untested anywhere in the module suite. | CONFIRMED | **Fixed** — added a fewer-than-requested fixture (3 requested, 1 returned, asserts the surviving record's own `Number`) and an empty-array success fixture (resolves `[]`, doesn't throw). |
| 5 | AC-04 | `getDocumentStatus`'s "exact comparison, no trimming/case-folding" claim (`spec.md` AC-04, `sad.md` §4 decision 3) had no counter-example test — both existing tests used either an exact match or a wholly unrelated `Number`, so a `.trim().toLowerCase()` bug in the match predicate would have passed the suite unnoticed. | CONFIRMED | **Fixed** — added a whitespace-padded `Number` (`" 20400048799000 "`) fixture asserting the convenience method resolves `undefined`, not a false match. |
| 6 | AC-03 | `getStatusDocuments`'s "one call, not split into several" claim (`spec.md` AC-03, test-plan row "single `fetch` call recorded") was never actually asserted with a call-count check — only the convenience method's test checked `toHaveBeenCalledTimes(1)`. Meaningful here since this repo already ships a client-side-looping batch elsewhere (`internet-document.deleteBatch`). | CONFIRMED | **Fixed** — added `expect(fetchMock).toHaveBeenCalledTimes(1)` to the existing AC-01/AC-03 test. |
| 7 | process (CLAUDE.md build commands / `internet-document` precedent) | No `.changeset/tracking-document.md` and no `docs/features/tracking-document/changelog.md` existed despite the feature adding 4 new public exports, unlike the `internet-document` precedent which ships both. | CONFIRMED | **Fixed** — added `.changeset/tracking-document.md` (`minor`) and `docs/features/tracking-document/changelog.md`. `docs/roadmap.md`'s status column intentionally left at `spec'd` — this repo's convention (per `internet-document`'s own history) flips a step to `shipped` only at PR merge, done by `sdd:ship`, not by review. |

### Resolutions requiring a judgment call (Tech Lead, this session)

- **API-key requirement (finding 1, part A):** kept the current default (library always sends the
  key, never independently validates it) and closed the open question — this doesn't change under
  either answer.
- **§6.1 security review (finding 1, part B):** performed in this session. No logging of PII, no
  key-validity check performed client-side, thrown error messages carry only the caller's own input
  waybill and a record count — never a response field — matching every sibling module's error
  contract exactly. Signed off with no code changes required.
- **Official-docs re-verification (finding 1, part C):** re-attempted with user-supplied
  `api-portal.novapost.com` URLs. Confirmed reachable and genuinely official, but documents a
  different, newer REST/JWT tracking API (`/shipments/tracking`, `numbers[]`) — no mention of
  `modelName`/`calledMethod`/`TrackingDocument`/`getStatusDocuments` anywhere in its index.
  `developers.novaposhta.ua` (the legacy docs' actual home) is still 403. Proceeded on the
  SDK cross-check, strengthened to 3 independently-agreeing sources (finding 2).

## Stage 2 — quality

Module (`src/modules/tracking-document/index.ts`) matches `src/modules/address/index.ts` and
`src/modules/common/index.ts` convention-for-convention: factory over `NovaPoshtaClient`, direct
`client.request()` delegation, the same `as unknown as Record<string, unknown>` cast used
everywhere else in this codebase. `NovaPoshtaApiError` reused unchanged, message-only construction
matching the `internet-document` precedent. §6.1's unscoped-PII posture is honoured in code: no
logging, no redaction, no key inspection. AC-11's module-boundary invariant (no `internet-document`
or `common` import) is a type-level/file-boundary check exactly as `contracts/public-api.md` §7
prescribes, not a runtime one.

## Verification after fixes

`npm run build` (ESM+CJS+DTS), `npm test` (**219 passed / 3 skipped**, up from 216 — the 3 new
AC-03/AC-04/AC-05 test cases), `npm run lint` all pass.

## Gate result: **PASS**

All 7 findings were fixed in this pass (3 stage-1 doc/process gaps, 3 stage-2 test-adequacy gaps,
1 release-artifact gap) — none deferred. `spec.md` §8 retains 2 genuinely open questions
(phone-match masking, the three-status-vocabulary reconciliation) by design; neither blocks this
feature's own acceptance criteria. No open stage-1 finding remains.
