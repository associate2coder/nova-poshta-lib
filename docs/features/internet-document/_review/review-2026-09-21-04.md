---
status: CHANGES REQUESTED
reviewer: sdd:reviewer (clean-context, independent)
date: "2026-09-21"
supersedes: review-2026-09-21-03.md
---

# Review — internet-document (fourth pass)

## Scope

- Diff: `main..HEAD` (full feature diff, `main` at `53167cd`), re-verified against
  `review-2026-09-21-03.md`'s 8 findings (P1–P8) plus a fresh end-to-end stage-1/stage-2 pass.
- Commits since round 3: `2ef4e30`, `040c30a`, `ef541d1`, `b261543`.
- Verified with `npx tsc --noEmit` (clean), `npx vitest run` (187 passed, 2 integration skipped),
  `npm run lint` (clean), plus targeted mutation probes.

## Stage 0 — disposition of round 3's P1–P8

| # | Round-3 finding | Verdict |
|---|---|---|
| P1 | `delete` sent `Documents`, not `DocumentRefs` | **RESOLVED** — mutation-proved; docs still stale in 2 spots → Q3 |
| P2 | Stale delete-contract reason-attribution text | **RESOLVED** |
| P3 | Undocumented `apiKey` shared-client exposure | **RESOLVED** |
| P4 | `sad.md` residual `request()`/warnings contradictions | **PARTIALLY RESOLVED** — building-block diagram + risk row fixed; §6 flow 2 and `public-api.md:376` still stale → Q2/Q3 |
| P5 | Unhandled rejection in body-discard cleanup | **RESOLVED** |
| P6 | Untested body-discard | **RESOLVED** |
| P7 | Print-verification blank-page gap undocumented | **RESOLVED** |
| P8 | Residual ADR-0001→0004 drift | **PARTIALLY RESOLVED** — the lines P8 cited are fixed; the same drift survives in 4 uncited spots in the same files → Q5 |

## Findings (this pass)

| # | Stage | Finding | Location | AC/contract | Resolution |
|---|---|---|---|---|---|
| Q1 | 1 | `delete`'s per-Ref reason matching uses plain substring search (`message.includes(ref)`); a Ref that's a textual prefix of another Ref (e.g. `waybill-1`/`waybill-10`) gets the wrong Ref's reason stamped on it — reproduced | `src/modules/internet-document/index.ts:135` | AC-08 | **Fix now** |
| Q2 | 2 | `sad.md` §6 flow 2 (delete) still shows `request()` and the wrong wire field `Documents`, and never shows `Reason` coming from warnings/errors | `sad.md:332-333` | AC-07, AC-08 | **Fix now** |
| Q3 | 2 | `contracts/public-api.md`'s transport table still lists `delete` as using `client.request()`; the contract never mentions `requestEnvelope()`, `NovaPoshtaClient.apiKey`, or the `NovaPoshtaSuccessEnvelope` export as added public surface | `contracts/public-api.md:376,432-433` | design fidelity | **Fix now** |
| Q4 | 2 | Contract + T1 DoD still mandate `Required<SaveInternetDocumentPayload>` for `UpdateInternetDocumentPayload`, contradicting the shipped hand-written variants that keep `BackwardDeliveryData` optional by design for AC-06 — the documented shape would actually break AC-06 | `contracts/public-api.md:182-188`; `tasks/t1-internet-document-domain-types.md:37-39,55-56` | AC-06 | **Fix now** |
| Q5 | 2 | Residual ADR-0001→0004 drift in the same two files P8 already touched: `tasks/_epic.md`'s Goal/Scope and `tasks/t1-...`'s Why still describe "two independent axes" | `tasks/_epic.md:8,18-19,57`; `tasks/t1-...:21` | AC-02, ADR-0004 | **Fix now** |
| Q6 | 2 | `delete([])` has no pre-check and no test either way; `test-plan.md` names the intended behavior (reach Nova Poshta, surface its decline) but nothing pins it | `test-plan.md:56` vs `src/modules/internet-document/index.ts:119-124` | AC-14 | **Fix now** |

Stage-1 trace is otherwise clean: all 12 user stories reach ≥1 AC and a `sad.md` §6 flow; all 19 ACs
reach code plus a non-tautological test; ADR-0002/0003/0004 match the shipped code (docs aside); no
`apiKey` leak surface beyond the by-design AC-13 print link.

## Resolution

All 6 findings → **Fix now**, resolved by the user via `AskUserQuestion`. Handed back to `implement`
as follow-up work on the same feature branch (no `/clear`).

## Verdict

**CHANGES REQUESTED**
