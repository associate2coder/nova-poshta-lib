---
status: PASS
reviewer: sdd:reviewer (clean-context, independent)
date: "2026-09-21"
supersedes: review-2026-09-21-05.md
---

# Review — internet-document (sixth pass)

## Scope

- Diff: `main..HEAD` (full feature diff, `main` at `53167cd`), re-verified against
  `review-2026-09-21-05.md`'s 4 findings (R1–R4) plus a fresh, skeptical end-to-end stage-1/stage-2
  pass over all 12 user stories and all 19 acceptance criteria — not just the surface R1–R4 touched.
- Commits since round 5: `14a6bd9`, `3e76636`.
- Verified with `npx tsc --noEmit` (clean), `npx vitest run` (190 passed, 2 integration skipped),
  `npm run lint` (clean).

## Stage 0 — disposition of round 5's R1–R4

| # | Round-5 finding | Verdict |
|---|---|---|
| R1 | `update()`'s public JSDoc claimed `BackwardDeliveryData` mandatory, superseded ADR-0001 language | **RESOLVED**, but the reworded text introduced a new, narrower inaccuracy — it now overpromises that BackwardDeliveryData is the *only* field that clears on omission (`SenderFlat`/`RecipientFlat` also clear) → this pass's F3 |
| R2 | No test supplied a real `BackwardDeliveryData` on update | **RESOLVED** — new test is well-formed, non-tautological, confirmed passing |
| R3 | Omitting a required leg field entirely was never pinned by `@ts-expect-error` | **RESOLVED** — probed directly: removing the directive surfaces `TS2741` on the declaration line, exactly as claimed |
| R4 | Contract didn't document the package-root export aliasing | **RESOLVED**, but the new prose mis-described *why* (`common`'s exports called "fixed literal sets" when they're record types) → this pass's F2 |

## Findings (this pass)

| # | Stage | Finding | Location | AC/contract | Resolution |
|---|---|---|---|---|---|
| F1 | 1 | `getDocumentPrice`/`getDocumentDeliveryDate` throw `NovaPoshtaApiError` on a success response with empty `data` — a real, working behavior, but `contracts/public-api.md`'s error table and `sad.md`'s flow-4 description both said this branch doesn't exist for these two methods | `src/modules/internet-document/index.ts:44-58` (`firstOrThrow`); `contracts/public-api.md` §6; `sad.md` flow 4 | AC-03, AC-04 | **Fix now** |
| F2 | 2 | R4's new contract note called `common`'s `ServiceType`/`CargoType`/`PayerType` "its own fixed literal sets" — they're actually `{ Ref?: string; Description?: string }` record types (`src/types/common.ts`), not literal unions; the alias itself was already correct | `contracts/public-api.md` §2 | AC-19 / US-12 | **Fix now** |
| F3 | 2 | R1's reworded `update()` JSDoc said only `BackwardDeliveryData` clears on omission; `SenderFlat`/`RecipientFlat` (Doors-ending legs) are also optional and also silently clear — undocumented, same AC-06 risk class | `src/modules/internet-document/index.ts:19-24`; `src/types/internet-document.ts:92-98`; `contracts/public-api.md` §3.2 | AC-06 | **Fix now** |
| F4 | 2 | `tasks/t2-save-and-update.md`'s Definition of Done still required a `ServiceType`/`CargoType`-combination compile guard, contradicting `tasks.json`'s already-corrected T2 wording | `tasks/t2-save-and-update.md:18,39-40` | AC-02, ADR-0004 | **Fix now** |
| F5 | 2 | Same residual two-axis language in `test-plan.md` (edge cases, test-data) and `sad.md`'s QG-1 scenario (cited ADR-0001 with no ADR-0004 narrowing) | `test-plan.md:55,65`; `sad.md` §10 QG-1 | AC-02, ADR-0004 | **Fix now** |
| F6 | 2 | `test-plan.md`'s AC-06 row didn't mention round 5's new pass-through test — the AC→test map was incomplete | `test-plan.md:38` | AC-06 | **Fix now** |
| F7 | 2 | `review-2026-09-21-05.md` recorded "191 passed" post-fix; actual count is 190 (R2 added one test, R3 added a case inside an existing test) | `_review/review-2026-09-21-05.md:57` | n/a | **Fix now** |

Verified clean this pass (no finding): AC-08's word-boundary per-Ref reason attribution intact and
untouched by round 5; `apiKey` still non-enumerable, interpolated only into the AC-13 print URL, never
into an error message; round 3's `DocumentRefs` wire-field fix still in place; the full §4 (12 user
stories) → §5 (19 ACs) → `sad.md` §6 → contract → `tasks.json` → implement chain traces end-to-end with
no story or AC dropping out.

## Resolution

All 7 findings → **Fix now**, resolved directly in this session (doc/JSDoc corrections only — no
behavior change, no new test required beyond what round 5 already added):

- F1: added a dedicated error-table row + flow-4 `alt` branch + method-doc notes for the calculators'
  empty-data-throws behavior in `contracts/public-api.md` and `sad.md`.
- F2: reworded the contract's package-root aliasing note to describe `common`'s exports correctly
  (runtime reference-list records, not literal sets).
- F3: extended `update()`'s JSDoc (both copies) and the contract to list all three omit-to-clear
  fields (`BackwardDeliveryData`, `SenderFlat`, `RecipientFlat`).
- F4: synced `tasks/t2-save-and-update.md`'s DoD wording to `tasks.json`'s already-corrected text;
  annotated the ADR-0001 link as superseded by ADR-0004.
- F5: reworded the three remaining "ServiceType/CargoType combination" spots in `test-plan.md`/`sad.md`
  to "ServiceType leg"; added the ADR-0004 citation to `sad.md`'s QG-1.
- F6: extended `test-plan.md`'s AC-06 row to cover the pass-through-test intent.
- F7: corrected the round-5 review record's post-fix test count to 190.

Verified after fixes: `npx tsc --noEmit` clean, `npx vitest run` 190 passed / 2 skipped, `npm run
lint` clean.

## Verdict

**PASS** (7 findings, all resolved in this same pass — doc/JSDoc accuracy only, no shipped behavior
changed; see commit(s) below)
