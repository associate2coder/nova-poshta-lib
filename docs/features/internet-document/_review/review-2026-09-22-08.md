---
status: PASS
reviewer: sdd:reviewer (clean-context, independent)
date: "2026-09-22"
supersedes: review-2026-09-22.md
---

# Review — internet-document (eighth pass)

## Scope

- Diff: `53167cd..HEAD` (full feature diff, `main` at `53167cd`), scoped to `internet-document`.
- A verification pass over round 7's 5 claimed fixes (`review-2026-09-22.md`), plus a fresh
  stage-1/stage-2 sweep and an honesty check on round 7's Nova-Poshta-docs claim.
- Verified with `npx tsc --noEmit` (clean), `npx vitest run` (192 passed, 2 integration skipped),
  `npm run lint` (clean) — all confirmed pre-existing at round 7's HEAD before this round's own
  fixes.

## Disposition of round 7's F1–F5

All five confirmed genuinely fixed, not cosmetic:

- **F1** — the staleness check was verified empirically (`touch src/index.ts` → all 8
  `build-surface.test.ts` cases fail with the new stale-build message; `npm run build` → green
  again).
- **F2** — `SaveCallArgument`/`UpdateCallArgument` are genuinely derived from
  `InternetDocumentModule`'s own method signatures via `Parameters<>`, not from
  `SaveInternetDocumentPayload` directly; the new `@ts-expect-error` cases provably fire (a clean
  `tsc --noEmit` proves an unused directive would itself be an error).
- **F3** — `sad.md` §9 and ADR-0001's own `status:` field now agree (`Superseded by 0004`).
- **F4** — `sad.md` flow 3's new `alt Documents is empty` branch matches
  `buildAndVerifyPrintLink`'s actual pre-fetch throw exactly.
- **F5** — `.changeset/internet-document.md` exists, well-formed, matching the repo's existing
  changeset shape.

## Findings (this pass)

| # | Stage | Finding | Location | AC/contract | Resolution |
|---|---|---|---|---|---|
| G1 | 2 | Round 7's docs-confirmation didn't propagate to `sad.md` (§2 regulatory note, §11 risk row) or `contracts/api-sync-report.md`, which still called the apiKey-embedding/single-combined-link assumptions "unconfirmed" after `spec.md` had already moved to "confirmed" — the exact drift class round 7's own F3 named | `sad.md:86,523`; `contracts/api-sync-report.md:65` | AC-11, AC-12, AC-13 | **Fix now** |
| G2 | 2 | `spec.md` §8 OQ-1 said the confirmed print-URL pattern "match[es] this module's shipped implementation exactly" — overstated; the source's `type/[html or pdf]` phrasing is a search-summary paraphrase, not a verbatim quote, and says nothing about a `copies` segment at all | `spec.md` §8 OQ-1 | AC-11, AC-12 | **Fix now** |
| G3 | 2 | Re-verifying G2's overclaim by re-fetching `serj1chen/nova-poshta-sdk-php`'s `getPrintLink()` *implementation* (round 7 had only re-checked its constant *declarations*) surfaced a real, shipped bug: `Copies` is not a `/copies/<value>` URL segment at all — Nova Poshta never defines one. `"fourfold"` repeats each Ref's `orders[]/<ref>` segment twice; every other value repeats it once. `buildAndVerifyPrintLink` had been sending a segment Nova Poshta doesn't understand for every caller who set `Copies` | `src/modules/internet-document/index.ts` (`buildAndVerifyPrintLink`); `src/types/internet-document.ts` (`PrintLinkPayload.Copies`) | AC-11, AC-12 | **Fix now** |
| G4 | 2 | `spec.md`/`sad.md` frontmatter `updated_at: "2026-09-21"` was stale relative to same-day (2026-09-22) edits both files already carried | `spec.md:5`, `sad.md:5` | n/a | **Fix now** |
| G5 | 2 | `tasks/tracker.md`'s seventh-pass section was inserted between the second and third passes, out of chronological order | `tasks/tracker.md` | n/a | **Fix now** |

Nova-Poshta-docs honesty check (not a separate finding — reported as requested): round 7's claims
were mostly honest — `developers.novaposhta.ua`'s continued 403 was correctly reported, not
overstated, and the documentation-vs-live-API distinction was drawn explicitly in `spec.md` §6.1.
The one overstatement is G2 above.

Stage 1: no findings. Re-verified round 6's F1–F7 against current file state: all seven still hold.

## Resolution

All 5 findings fixed directly in this session:

- **G1:** `sad.md:86` and the §11 risk row now cite the seventh-pass confirmation instead of
  "unconfirmed"; `contracts/api-sync-report.md`'s print-link row now cites the primary source
  alongside the PHP SDK, scoped to what it actually confirms.
- **G2 & G3:** `buildAndVerifyPrintLink` no longer emits a `/copies/<value>` segment; `Copies:
  "fourfold"` now repeats each Ref's URL segment twice, matching `getPrintLink()`'s real behavior.
  Existing `Copies` test corrected (it asserted the old, wrong URL); a new test covers the
  `fourfold` case. `PrintLinkPayload.Copies`'s doc comment, `contracts/public-api.md` §3.7,
  `ADR-0003`'s amendment log, and `spec.md` §8 OQ-1 all updated to describe the real mechanism and
  scope the "exactly" claim down to what's genuinely confirmed (the `orders[]`-repetition +
  trailing `apiKey` structure).
- **G4:** both frontmatter blocks bumped to `2026-09-22`.
- **G5:** the seventh-pass section moved after the sixth-pass section; this eighth-pass section
  appended after it.

Verified after fixes: `npx tsc --noEmit` clean, `npx vitest run` 193 passed / 2 skipped (192 + 1 new
`fourfold`-copies case), `npm run lint` clean.

## Verdict

**PASS.** All findings are doc-accuracy and one genuine-but-narrow behavior fix (an unrecognized
URL segment Nova Poshta never processed, now replaced with the real Ref-repetition mechanism) —
no AC in scope is unsatisfied, and every user story still traces end-to-end. Per this round's own
recommendation: the last three consecutive rounds' yield has been increasingly narrow (doc drift,
then a single real-but-small bug found only by re-verifying a docs claim more rigorously than
strictly necessary). This is the natural point to stop re-reviewing and ship.
