---
status: CHANGES REQUESTED
reviewer: sdd:reviewer (clean-context, independent)
date: "2026-09-21"
supersedes: review-2026-09-21-04.md
---

# Review — internet-document (fifth pass)

## Scope

- Diff: `main..HEAD` (full feature diff, `main` at `53167cd`), re-verified against
  `review-2026-09-21-04.md`'s 6 findings (Q1–Q6) plus a fresh end-to-end stage-1/stage-2 pass.
- Commits since round 4: `dbc5a46`, `c1457a2`.
- Verified with `npx tsc --noEmit` (clean), `npx vitest run` (189 passed, 2 integration skipped),
  `npm run lint` (clean), plus targeted regex-escaping and TS-error-shape probes.

## Stage 0 — disposition of round 4's Q1–Q6

| # | Round-4 finding | Verdict |
|---|---|---|
| Q1 | Plain-substring reason match mis-attributes on Ref-prefix collision | **RESOLVED** — word-boundary regex verified correct against 15 cases incl. regex-metacharacter Refs (properly escaped) |
| Q2 | `sad.md` §6 flow 2 stale `request()`/`Documents` | **RESOLVED** |
| Q3 | `contracts/public-api.md` transport table stale, missing new public surface | **RESOLVED** |
| Q4 | Contract + T1 DoD mandated `Required<SaveInternetDocumentPayload>`, contradicting shipped optional `BackwardDeliveryData` | **PARTIALLY RESOLVED** — contract and task-file docs fixed; the same claim survived verbatim in the `update()` JSDoc in `src/modules/internet-document/index.ts` → R1 |
| Q5 | Residual ADR-0001→0004 two-axis language in `tasks/_epic.md`/`tasks/t1-...` | **RESOLVED** |
| Q6 | `delete([])` behavior undocumented by test | **RESOLVED** — pins exactly the documented behavior (reaches Nova Poshta, no client-side pre-check) |

## Findings (this pass)

| # | Stage | Finding | Location | AC/contract | Resolution |
|---|---|---|---|---|---|
| R1 | 2 | `update()`'s public JSDoc still said `BackwardDeliveryData` was mandatory and used ADR-0001's superseded "ServiceType/CargoType combination" language — the one copy round 4's Q4/Q5 fix missed (it touched the contract and task file, not this doc-in-code, which is what a consumer actually sees in autocomplete) | `src/modules/internet-document/index.ts:19-23` | AC-06, ADR-0004 | **Fix now** |
| R2 | 1 | No test ever supplied a real (non-`undefined`) `BackwardDeliveryData` on update — only the omit-to-clear path was tested; a bug that unconditionally stripped the field would pass the whole suite | `test/unit/modules/internet-document.test.ts:97-110` | AC-06; spec §7 write-safety | **Fix now** |
| R3 | 1 | The compile-time guarantee that omitting a required leg field (not just mixing legs) fails to compile was documented (`test-plan.md`, spec §6 NFR 3) but never pinned by a `@ts-expect-error` — TS's excess-property short-circuit meant every existing negative case tested only the "wrong field present" half | `test/unit/types/internet-document.test.ts:81-136` | AC-02; spec §6 NFR 3 | **Fix now** |
| R4 | 2 | Contract documents package-root exports as bare `ServiceType`/`CargoType`/`PayerType`; `src/index.ts` actually aliases them `InternetDocumentServiceType`/`InternetDocumentCargoType`/`InternetDocumentPayerType` to avoid colliding with `common`'s exports — undocumented in any feature artifact | `src/index.ts:96-99` vs `contracts/public-api.md:43-55` | US-12 / AC-19 discoverability | **Fix now** |

Stage-1 trace is otherwise clean: all 12 user stories reach ≥1 AC and a `sad.md` §6 flow; all 19 ACs
reach real code plus a non-tautological test. `delete`'s AC-08 path (Ref reconciliation, per-Ref
reason attribution with correct escaping) verified correct end to end. `apiKey` exposure confined to
the documented AC-13 print-link path — non-enumerable, read only where documented, never interpolated
into an error message. `requestEnvelope()` shares `sendRequest()`'s single error-mapping path, so the
`NovaPoshtaApiError` convention (CLAUDE.md) holds uniformly.

## Resolution

All 4 findings → **Fix now**, resolved directly in this session (small, targeted doc/test fixes on
the same feature branch, no `/clear`):

- R1: reworded `update()`'s JSDoc to match `src/types/internet-document.ts:92-98`'s correct language.
- R2: added a test asserting a supplied `BackwardDeliveryData` reaches `methodProperties` intact.
- R3: added a `@ts-expect-error` case for a `SaveWarehouseToWarehousePayload` missing a required leg
  field entirely (`RecipientAddress`), confirmed via a standalone `tsc` probe that TS reports this as
  `TS2741` on the declaration line.
- R4: documented the three package-root export aliases in `contracts/public-api.md` §2.

Verified after fixes: `npx tsc --noEmit` clean, `npx vitest run` 190 passed / 2 skipped (correction,
round 6: R2 added one new test; R3 added a case inside an existing test, not a new one — the
originally-recorded 191 was off by one), `npm run lint` clean.

## Verdict

**CHANGES REQUESTED** (all 4 findings fixed in this same pass — see commit(s) below)
