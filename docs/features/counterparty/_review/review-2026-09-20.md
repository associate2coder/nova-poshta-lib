# Review record — counterparty (2026-09-20)

## Scope

Diff: `ee4962c..HEAD` (tip of the prior `address` feature → tip of `counterparty`), restricted to
`src/` and `test/` — 7 files changed, 1,107 insertions across `src/modules/counterparty/index.ts`,
`src/types/counterparty.ts`, `src/index.ts` wiring additions, `test/unit/modules/counterparty.test.ts`,
`test/unit/types/counterparty.test.ts`, `test/unit/build-surface.test.ts`, `test/unit/smoke.test.ts`.

Upstream artifacts read directly by the reviewer: `spec.md` (§4 11 user stories, §5 17 ACs, §6 NFRs,
§6.1 security, §8 open questions), `sad.md` (§5 building blocks, §6 two sequence flows, §9 ADR-0001,
§11 risks), `contracts/public-api.md`, `contracts/api-sync-report.md`, `tasks.json` (T1–T10).

Independent reviewer: `sdd:reviewer` subagent, clean context, read-only. Verified green first
(`npm run build`, `tsc --noEmit`, `npm test` — 112 passed, `npm run lint`), then mutation-tested the
type-level guards (AC-03/04/05/09) by weakening each in a scratch copy and confirming
`TS2578: Unused '@ts-expect-error'` fired — all four are genuinely load-bearing, not decorative.

## AC / user-story trace

All 17 ACs (AC-01..AC-17) and all 11 user stories (US-01..US-11) trace end-to-end: spec → sad.md §6
flows → tasks.json → code → test. No AC or user story dropped out of the chain. `getCounterpartyAddresses`
correctly imports `address`'s own `SavedAddress` type rather than redefining it. AC-07's
`firstOrUndefined` helper correctly covers all 6 write methods. AC-13 (no cross-context enforcement)
holds by construction — no second call anywhere in the write paths. No runtime coupling to `address`
(`import type` only).

## Findings — all resolved

| # | Stage | Finding | Resolution |
|---|---|---|---|
| 1 | 1 (AC) | AC-17's build-surface check matched `save`/`update`/`delete` against the whole declaration file, where `address`'s module already declares those names — a deleted counterparty method would go undetected | **Fixed** — scoped the match to the `interface CounterpartyModule { ... }` block only (`test/unit/build-surface.test.ts`). Mutation-verified: deleting `save` from the interface+impl now fails the test (previously passed). |
| 2 | 1 (AC) | AC-12's no-caching test called `getCounterparties` with two *different* search strings — an argument-keyed cache would pass unchanged | **Fixed** — test now calls the identical lookup twice and asserts two fetches with a mock returning a different result the second time (`test/unit/modules/counterparty.test.ts`). Mutation-verified: adding an argument-keyed cache now fails the test. |
| 3 | 1 (AC) | Spec §8 OQ-2 (verify the live wire method name + the `CounterpartyType` discriminant assumption) was due *before* `sdd:implement`, but was never discharged before implementation shipped | **Deferred** — `spec.md` §8 OQ-2 and `sad.md` §11 updated: status marked still-open as of this review, due date moved to before `sdd:ship counterparty`. Needs a live API key, not more code. |
| 4 | 1 (AC) | The test plan's one opt-in integration test (AC-01/AC-02, gated by `NOVA_POSHTA_TEST_API_KEY`) was never written; no task tracked it as missing | **Fixed** — added `test/integration/counterparty.test.ts` (`describe.skipIf(!apiKey)`), matching the CLAUDE.md convention. Confirmed it skips cleanly without a key and is picked up by `vitest.config.ts`'s existing include glob. |
| 5 | 2 (quality) | The "SavedAddress is reused, not redefined" test never referenced the counterparty module or its types — a local redefinition would pass identically | **Fixed** — rewrote as a signature-level check assigning `CounterpartyModule["getCounterpartyAddresses"]` against an explicit `(filters) => Promise<SavedAddress[]>` annotation (`test/unit/types/counterparty.test.ts`). |
| 6 | 2 (quality) | Three AC mislabels: a wiring smoke test tagged `AC-12` (no-caching) instead of `AC-17` (discoverability); `tasks.json` T7 claimed `AC-12` for the same reason; an `updateContactPerson` happy-path test tagged `AC-10` (delete) instead of `AC-09` (update); `tasks.json` T10 (README) claimed `AC-17`, which is specifically the published-`.d.ts` gate, not documentation | **Fixed** — retagged `test/unit/smoke.test.ts` and `tasks.json` T7 to `AC-17`; retagged the `updateContactPerson` test to `AC-09`; dropped T10's AC claim (`acs: []`, no AC covers the README) and adjusted its task doc's rationale. |
| 7 | 2 (quality) | AC-01's pass-through-when-supplied pagination behavior was tested only for `getCounterparties`; `getCounterpartiesCatalog` and `getCounterpartyContactPersons` had no test confirming their `Page` parameter reaches the request | **Fixed** — extended both lookups' existing fixtures to include and assert `Page` (`test/unit/modules/counterparty.test.ts`). |

No stage-1 or stage-2 findings were dismissed as "not an issue" — all 7 were either fixed or
explicitly deferred with an owner and due date.

## Verification after fixes

- `npm run build` — clean
- `npx tsc --noEmit` — clean
- `npm test` — 112 passed, 1 skipped (the new integration test, correctly skipped without an API key)
- `npm run lint` — clean
- Mutation-verified findings #1 and #2 fail correctly when the underlying bug is reintroduced, then
  confirmed a byte-identical revert (`diff` against a pre-mutation backup)

## Gate result

**PASS.**
