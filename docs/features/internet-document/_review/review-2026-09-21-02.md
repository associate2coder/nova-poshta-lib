---
status: CHANGES REQUESTED
reviewer: sdd:reviewer (clean-context, independent)
date: "2026-09-21"
supersedes: review-2026-09-21.md
---

# Review — internet-document (re-review)

## Scope

- Diff: `94d3893..5a32f77` (fix commits addressing `review-2026-09-21.md`'s 10 "Fix now" findings), verified against the full feature diff `f04dd5f..5a32f77` for regressions.
- Commits: `6276707` (AC-02 discriminant), `c8c5658` (apiKey leak), `df52553` (AC-08 reason), `f696600` (print-link assertions), `c866619` (README AC-05 guard), `5a32f77` (opt-in integration suite).
- Verified with `npm run typecheck` / `npm test` / `npm run lint` (183 passed, 2 integration skipped) plus targeted mutation testing of the fixed assertions.

## Stage 0 — disposition of the prior review's 10 "Fix now" findings

| # | Prior finding | Verdict |
|---|---|---|
| 1 | `CargoTypeDetail` / cargo-axis gap | **RESOLVED** by narrowing (ADR-0004, Accepted) — doc propagation gap → N1 |
| 2 | Sender leg not discriminated | **RESOLVED** — mutation-proved |
| 3 | `apiKey` enumerable | **RESOLVED** — `Object.defineProperty(enumerable:false)`, falsifiable test |
| 4 | AC-08 hardcoded reason | **RESOLVED** — reads `envelope.warnings/errors`; attribution quality → N3 |
| 5 | AC-02 type tests: one axis, wrong location | **PARTIALLY RESOLVED** — moved to correct file, but recipient-leg negative dropped → N2 |
| 6 | Print-link tests non-falsifiable | **RESOLVED** — exact-URL assertions, mutation-proved |
| 7 | README `waybill!.Ref` | **PARTIALLY RESOLVED** — same hazard remains one line above → N4 |
| 8 | AC-06 test non-falsifiable | **RESOLVED** — mutation-proved |
| 9 | Missing integration suite | **PARTIALLY RESOLVED** — AC-09 covered; other 8 ACs' integration rows downgraded in test-plan.md with owner+due (non-blocking, precedent-consistent) |
| 11 | Print verification body leak | **RESOLVED** by switching to `HEAD` — introduced an unverified assumption → N5 |
| 10 (deferred) | AC-19 shallow build-surface check | Still recorded in spec.md §8, owner: Tech Lead |

## Findings (new/residual, this pass)

| # | Stage | Finding | Location | AC/contract | Resolution |
|---|---|---|---|---|---|
| N1 | 2 | ADR-0004's narrowing never propagated to sad.md (still describes two-axis payload as the design), the ADR table (still lists only 0001 Accepted), `contracts/api-sync-report.md:52` (cites deleted `CargoTypeDetail`), and `tasks.json` T1/T2 `dod` (still cites ADR-0001's two-axis intersection) | `sad.md:28,67,160-168,220,412,428,519-520`; `contracts/api-sync-report.md:52`; `tasks.json` T1/T2 | AC-02, ADR-0004 | **Fix now** |
| N2 | 1 | AC-02's discriminant guard is only tested on the sender side; a recipient-leg payload accepting fields from the wrong delivery method (e.g. `RecipientAddress` on a `*Doors` variant) passes `tsc` and the full suite unnoticed | `test/unit/types/internet-document.test.ts:33-177`; types at `src/types/internet-document.ts:42-44,54-60` | AC-02, spec.md:261 NFR | **Fix now** |
| N3 | 1 | AC-08 reason attribution: `warnings.join() \|\| errors.join()` drops errors whenever any warning exists, and the one joined string is stamped onto every rejected Ref regardless of which Ref it names | `src/modules/internet-document/index.ts:125-131` | AC-08, ADR-0002 | **Fix now** |
| N4 | 2 | README's AC-05 guard covers `waybill.save()` but the line above still double-asserts `recipient!.Ref!` from `counterparty.save()`, which has the identical undefined-on-success hazard | `README.md:52` | AC-05, AC-17 | **Fix now** |
| N5 | 2 | Body-leak fix switched print-link verification to `HEAD`, an unconfirmed assumption about Nova Poshta's print endpoint (spec.md OQ-1 flags the wire shape as unconfirmed); if the host doesn't support `HEAD`, print calls fail 100% in production with all tests green | `src/modules/internet-document/index.ts:79-92`; `adr/0003-construct-then-verify-print-links.md`; spec.md:288 | AC-11, AC-12, §8 OQ-1 | **Fix now** — switch to `GET` + discard body |
| N6 | 2 | AC-08 fix added a new public `requestEnvelope()` method to the shared core client, contradicting sad.md's explicit "client.ts unchanged by this feature" / "core client stays untouched" and spec.md's non-goal + OQ-2 (exposing success-path warnings recorded as "not fixed in this feature") | `src/client.ts:19-42,110-116`; sad.md:143-146,176-184,203,263; spec.md:61,289 | §3 non-goal, §8 OQ-2 | **Fix now** — record the decision in sad.md/ADR, code change itself is fine |
| N7 | 2 | `NovaPoshtaSuccessEnvelope<T>` (return type of the new client method) isn't re-exported from the package entry, so consumers can't name it | `src/index.ts:1-3`; `src/client.ts:23-27` | public surface consistency | **Fix now** |

## Resolution

All 7 findings → **Fix now**, resolved by the user via AskUserQuestion. Handed back to `implement` as follow-up work on the same feature branch (no `/clear`).

## Verdict

**CHANGES REQUESTED**
