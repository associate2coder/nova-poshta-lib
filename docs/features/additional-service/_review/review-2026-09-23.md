---
status: CHANGES REQUESTED
reviewer: "independent clean-context review (sdd:reviewer)"
date: "2026-09-23"
---

# Review — additional-service (2026-09-23)

## Scope

`git diff 2da17d1..ad29271` (feature commits only — isolated from the earlier, already-shipped
`scan-sheet` history on the same branch) — 40 files changed (+5859/-25):

```
CONTEXT.md                                                          |    5 +-
README.md                                                           |   26 +-
docs/features/additional-service/.route                             |    1 +
docs/features/additional-service/.size                              |    1 +
.../adr/0001-expose-the-envelopes-info-field-via-requestenvelope.md |   84 ++
.../adr/0002-promote-firstorthrow-into-the-core-client-as-requestfirst.md | 88 ++
docs/features/additional-service/contracts/api-sync-report.md       |  105 ++
docs/features/additional-service/contracts/public-api.md            |  688 +++
docs/features/additional-service/sad.md                             |  873 ++++
docs/features/additional-service/spec.md                            |  355 ++
docs/features/additional-service/tasks.json                         |  162 +++
docs/features/additional-service/tasks/*.md (18 files)              |  ...
docs/roadmap.md                                                     |   12 +-
src/client.ts                                                       |   22 +
src/index.ts                                                        |   40 +
src/modules/additional-service/index.ts                             |  246 ++
src/modules/internet-document/index.ts                              |   22 +-
src/types/additional-service.ts                                     |  357 ++
test/unit/build-surface.test.ts                                     |   41 +
test/unit/client.test.ts                                            |   50 +
test/unit/modules/additional-service.test.ts                        | 1465 +++
test/unit/types/additional-service.test.ts                          |  460 ++
```

## Stage 1 — AC / user-story trace

Verified clean: all 23 ACs in `spec.md` §5 trace spec → `sad.md` §6 flow table → `tasks.json`
(union of `acs` = AC-01..AC-23, no gap) → code → a named test. All 19 methods are implemented,
exported from `src/index.ts`, and asserted in `build-surface.test.ts:286-300`. The shared
error-contract fixture (`additional-service.test.ts:1282-1384`) genuinely exercises all 19 methods
× 4 failure modes — no silent no-op. The `internet-document` refactor is behavior-preserving
(`requestFirst` reproduces `firstOrThrow`'s error string byte-for-byte; the existing suite was not
touched or weakened). ADR-0001 and ADR-0002 are implemented exactly as decided. 404 tests pass,
lint clean.

## Findings

All five resolved **Fix now** with the user.

1. **[stage-1 · CONFIRMED] No upstream struct/schema is quoted anywhere in this feature's
   artifacts** — `spec.md:12,68,87` and `contracts/public-api.md:477` cite SDKs and struct names
   (`platx`'s `SaveChangeEWReq`, `sirkostya009`'s `ChangeEWRequest`) *by name only*;
   `contracts/api-sync-report.md:17-63`'s entire `Origin` column is self-referential (`spec §1 row
   N` — a generated artifact citing a generated artifact). CLAUDE.md's API-contract sourcing
   policy requires "quote or list the exact upstream struct/schema used for the decision, not just
   cite an SDK by name." The repo's own precedent does this correctly:
   `docs/features/scan-sheet/spec.md:74-80` pastes the Go struct with its `json:"…"` tags. The only
   verbatim upstream quote in the whole feature is one Ukrainian sentence for `delete`
   (`spec.md:38`).
   **Resolution:** Fix now — paste the actual `AdditionalServiceGeneral` request/response schemas
   (official docs capture, or the two Go structs) into `spec.md` §1 / `api-sync-report.md`.

2. **[stage-1 · CONFIRMED, most severe] `ReturnAddressRef` mapping shipped with a self-declared
   open blocker** — `sad.md` §11 row 1 states verbatim that the `ReturnAddressOption.Ref` ↔
   `ReturnAddressRef` mapping "genuinely blocks `/sdd:api` … and `/sdd:review` cannot PASS with it
   still open (CLAUDE.md sourcing policy)." It is still open: `public-api.md` §10 item 1,
   `spec.md:351` (marked "this one blocks, not just tracks"), and `api-sync-report.md:88-95`
   resolves it as "Accept the design as-is, track as an open risk." `spec.md:351` records a
   *contrary* third-party source (`ReturnAddressRef` may expect a counterparty-address reference,
   not the return-address ref used) — a cross-source discrepancy, which CLAUDE.md says blocks ship
   outright, not merely tracks. The unverified assumption is hard-coded at
   `src/modules/additional-service/index.ts:241` (`ReturnAddressRef: options[0]!.Ref`) and shipped
   with only a README "open risk" note (`README.md:124-129`). AC-20 is implemented against an
   unverified wire contract.
   **Resolution:** Fix now — confirm against official Nova Poshta docs or a live test key before
   merge.

3. **[stage-1 · CONFIRMED] Three request fields ship with zero sources** — `updateReturn`/
   `updateRedirect`'s `Ref` field (`src/types/additional-service.ts:110,232`,
   `src/modules/additional-service/index.ts:155-160,198-203`), `CheckReturnEditPossiblePayload.Address`
   (`types.ts:33`), and `createRedirect.ServiceType` (`types.ts:213`) are all marked `low` in
   `api-sync-report.md:33` and admitted unconfirmed in `spec.md:70` ("neither cross-checked SDK
   exposed `update`'s own struct directly this session"). Under CLAUDE.md this is a sourcing-policy
   violation, not a tolerable open question — and since tests only mock-echo whatever the module
   sends (`additional-service.test.ts:360-374`), a wrong field name would not be caught.
   **Resolution:** Fix now — trace these three fields to official docs or 2 agreeing third-party
   sources.

4. **[stage-2 · CONFIRMED] Unchecked cast makes `CheckReturnEditPossibleResult.info` lie** —
   `src/modules/additional-service/index.ts:142` does `info: envelope.info as ReturnEditInfo`,
   while the client types it `info?: unknown` (`src/client.ts:29`) and
   `src/types/additional-service.ts:50` declares it non-optional. If Nova Poshta omits `info` on
   this response, callers get `undefined` behind a non-optional type and a raw `TypeError` — not
   the library's `NovaPoshtaApiError` — breaking CLAUDE.md's single-error contract. No test covers
   the info-absent case (`additional-service.test.ts:114-130` always supplies it), and this
   contradicts `sad.md` §4 decision 6's defensive-typing rule the module applies everywhere else.
   **Resolution:** Fix now — make `info?: ReturnEditInfo` optional and add a test for the
   omitted-`info` shape.

5. **[stage-2 · CONFIRMED, low] No changeset for the module** — `.changeset/` has one file per
   previously shipped module (`common-reference-lists.md`, `internet-document.md`,
   `tracking-document.md`, `scan-sheet.md`) but none for `additional-service`; the 19-method public
   surface added in `src/index.ts:138-176` would publish with no version bump.
   **Resolution:** Fix now — add `.changeset/additional-service.md` (minor), matching the pattern
   used by the other modules.

## Clean, explicitly verified

Module shape matches `address`/`counterparty`/`internet-document`/`scan-sheet` (factory over
`NovaPoshtaClient`, `src/types/<domain>.ts`, mocked-`fetch` unit tests); error handling routes
exclusively through `NovaPoshtaApiError` elsewhere in the module; public surface wired and asserted
in `build-surface.test.ts`; ADR-0001 (envelope `info` exposure) and ADR-0002 (`requestFirst<T>()`
promoted to core client) both implemented exactly as decided, with the `internet-document` refactor
preserving its existing error string byte-for-byte.

## Gate result (round 1)

**CHANGES REQUESTED** — 5 findings, all resolved "Fix now" with the user. Findings 1–3 are
stage-1/API-contract-sourcing severity per CLAUDE.md and block ship until resolved; findings 4–5
are stage-2 and actionable now. Next: `/sdd:implement additional-service` for the fixes (no
`/clear`), then re-review the changed surface.

## Round 1 fixes (commits `796a6cb`, `b567451`)

- **Findings 1/2/3** — the user retrieved `developers.novaposhta.ua`'s `AdditionalServiceGeneral`
  page directly in their own browser (automated fetch stays Cloudflare-blocked) and pasted its full
  content back. `spec.md` §1 gained a new "Official documentation quotes (captured 2026-09-23,
  verbatim)" subsection quoting the actual request/response JSON for every previously-unsourced
  item. Resolved: the `ReturnAddressRef` mapping (§8 OQ-1, `sad.md` §11 row 1 — no longer
  "blocking"), `CheckReturnEditPossiblePayload.Address`'s shape (a plain string), the full
  `CheckRedirectEditPossiblePayload` field list (14 named optional fields), `updateReturn`/
  `updateRedirect`'s `Ref` field name (§8 OQ-5), and whether ChangeEW orders have an `update` method
  (confirmed: no). `spec.md` §8, `sad.md` §11, `public-api.md` §10, and `api-sync-report.md`'s
  confidence table were all updated to cite the quoted docs instead of the prior SDK-name-only
  citations; `README.md`'s "OPEN RISK" comment on `createReturnIfPossible` was replaced with a
  docs-confirmed note. Only `CreateRedirectPayload.ServiceType`'s full enum stays genuinely open
  (one example value confirmed, not the complete set) — `public-api.md` §10 item 5, unchanged.

  The same docs capture surfaced 4 real defects the original SDK-only sourcing missed, all fixed in
  `src/types/additional-service.ts`: `ReturnAddressOption.NonCash` is a `boolean`, not a `"0"`/`"1"`
  string; `OrderPricingEstimate.Pricing.FirstDayStorage` is a datetime `string`, not a `number`;
  `RedirectOrderListItem` was missing `DocumentNumber` entirely; and `update`'s own wire call
  requires `OrderType` — `updateReturn`/`updateRedirect` (`src/modules/additional-service/index.ts`)
  now set it internally ("orderCargoReturn"/"orderRedirecting"), matching `createReturn`/
  `createRedirect`'s existing discriminant-stripping pattern; the original implementation omitted
  it entirely. `ReturnEditOption` and `CheckRedirectEditPossiblePayload` were tightened from
  loosely-typed index signatures to the now-confirmed named fields.

- **Finding 4** — `CheckReturnEditPossibleResult.info` is now `ReturnEditInfo | undefined`
  (`src/types/additional-service.ts`); `checkReturnEditPossible` (`src/modules/additional-service/index.ts`)
  unwraps the envelope's `info` defensively (`Array.isArray(envelope.info) ? envelope.info[0] :
  undefined`) rather than force-casting it, since the official docs' own example confirms the wire
  wraps `info` in a single-element array, not a bare object as the original implementation assumed.
  Added a test asserting `info: undefined` (not a thrown `TypeError`) when the envelope omits
  `info` entirely.

- **Finding 5** — added `.changeset/additional-service.md` (minor), matching every other shipped
  module's pattern.

All fixes gated together (`npm run lint`, `npx tsc --noEmit`, `npm run build`, `npm test` — all
clean, 405 tests passing, 0 failures) and committed in this working session.

## Round 2 — re-review (`git diff ad29271..HEAD`)

Independent clean-context reviewer re-ran over the round-1 fix commits (`796a6cb`, `b567451`) plus
a fresh full stage-1 AC trace and stage-2 pass over the current HEAD. Gate re-run independently:
405 tests passing, 0 failures, lint clean, `tsc --noEmit` clean.

### Round-1 findings — verification

1. **PARTIALLY RESOLVED.** `spec.md` now quotes the official docs verbatim for most previously
   SDK-only items, and `api-sync-report.md` cites the quoted docs instead of self-referential
   `spec §1 row N`. But `createWaybillEdit`'s request set (`spec.md:70,219`,
   `contracts/public-api.md:502`, `src/types/additional-service.ts:336`) and
   `checkWaybillEditPossible`'s field list (`api-sync-report.md:60-61`) still justify themselves by
   naming two SDKs (`platx/go-nova-poshta`'s `SaveChangeEWReq`, `sirkostya009/go-novapost`'s
   `ChangeEWRequest`) with no struct or field list quoted — the exact practice CLAUDE.md's sourcing
   policy forbids.
2. **RESOLVED IN SUBSTANCE, NOT IN INTERNAL CONSISTENCY.** The `ReturnAddressRef` mapping is
   genuinely confirmed against quoted docs (`spec.md:116-135`, `sad.md:843` §11 row 1 struck
   through/Closed, `public-api.md:26-36,701-702`, `api-sync-report.md:27,94-101`,
   `README.md:125-129`, code at `src/modules/additional-service/index.ts:247`). But `sad.md` still
   contradicts its own closed §11 in six places: `sad.md:348` (Flow 1 diagram: "unconfirmed — see
   §11"), `sad.md:389`, `sad.md:45,47-53` (the "Decision override — proceeding past a blocking open
   question" paragraph), `sad.md:36,197-206` (§4 decision 6, describing the now-tightened edit-check
   types as still loosely-typed), `sad.md:812-822` (§10 QG-3, misstating the shipped risk posture).
3. **RESOLVED** for the three named fields (`updateReturn`/`updateRedirect`'s `Ref`,
   `CheckReturnEditPossiblePayload.Address`, `createRedirect.ServiceType`'s field name) — each now
   quoted or explicitly and honestly flagged as an open *value*-set gap (`ServiceType`'s enum),
   which is not a sourcing-policy violation.
4. **RESOLVED.** `info?: ReturnEditInfo`, defensive `Array.isArray` unwrap replacing the force-cast,
   a genuinely discriminating test for the omitted-`info` case. No other unguarded response-shape
   cast remains in the module.
5. **RESOLVED.** `.changeset/additional-service.md` exists, `minor`, matches the repo's pattern.

### New findings from the fresh full pass

6. **[stage-1] `OnlyGetPricing` sent as the string `"1"` with zero cited source, contradicting the
   one docs quote that shows this field** — `src/modules/additional-service/index.ts:161,203` send
   `"1"`; the sole verbatim quote containing this field (`spec.md:141`, the `update` example) shows
   `"OnlyGetPricing": true` (a JSON boolean). `api-sync-report.md` has no origin row for this field
   at all. Same defect class the docs capture just fixed for `NonCash`. CLAUDE.md: "any discrepancy
   between sources blocks ship."
7. **[stage-2] Stale JSDoc** — `src/modules/additional-service/index.ts:37` still says `NonCash`
   is "a raw wire string," contradicting the now-`boolean` type and its own type-level comment.
8. **[stage-2] `sad.md` §7/§12 still describe `checkReturnEditPossible`'s `info` as non-optional**
   (`sad.md:759,872`) after finding 4 made it `info?:`.
9. **[stage-2, low] No test pins the bare-object (non-array) `info` case** — contract-correct
   silent-drop behavior, but unproven by a test.

### Resolution (with the user)

- **Findings 1 (residual) and 6** — stage-1, CLAUDE.md sourcing-policy blockers, no defer option
  offered per policy. **Fix now** — trace `createWaybillEdit`/`checkWaybillEditPossible` to a quoted
  struct/schema and reconcile `OnlyGetPricing`'s type against the quoted `true` example.
- **Findings 2 (stale cross-refs), 7, 8** — stage-2 doc/comment staleness. **Fix now** — sweep
  `sad.md` §1/§4/§6/§10 the same way §11 was swept; fix the `NonCash` JSDoc; mark `info` optional in
  `sad.md` §7/§12.
- **Finding 9** — stage-2, low. **Fix now** — add a test pinning the silent-drop behavior for a
  bare-object `info`.

## Gate result (round 2)

**CHANGES REQUESTED** — 3 of 5 round-1 findings fully resolved (3, 4, 5); finding 1 partially
resolved (ChangeEW request set still SDK-name-only); finding 2 resolved in substance but with 6
stale cross-references in `sad.md`. One new stage-1 sourcing gap (`OnlyGetPricing`). Nothing
regressed. All 4 outstanding items resolved **Fix now** with the user. Next:
`/sdd:implement additional-service` for the fixes (no `/clear`), then re-review the changed
surface.

## Round 3 — exhaustive whole-feature audit (pre-emptive, before round-2 fixes were applied)

At the user's request, before running `implement` on round 2's fixes, three parallel clean-context
reviewers ran an exhaustive whole-feature (not diff-scoped) pass — a full field-by-field
API-sourcing audit across all 19 methods, a full stage-1 AC/use-case trace, and a stage-2
quality/test-adequacy pass — to surface everything in one round instead of drip-feeding across
more cycles. This confirmed round 2's 4 unresolved items are still unfixed (expected — no
`implement` ran between rounds 2 and 3) and found the sourcing gap is far larger than either prior
round surfaced.

### Sourcing audit — scale

Only 9 of the ~30 distinct request/response shapes in the feature have a genuine verbatim docs
quote anywhere in `spec.md`/`api-sync-report.md`. Roughly **82 response fields and 48
request/wire values ship unsourced** — the ChangeEW cluster and `OnlyGetPricing` (round 2's
finding) are a small fraction of the defect class, not most of it. Newly identified unsourced or
misgraded surfaces (all **[stage-1]**, cited `file:line` in the full agent reports above this
section's summary):

- `createRedirect`/`calculateRedirect`'s entire 14-field request (`src/types/additional-service.ts:235-251`) — money-bearing (`PaymentMethod`, `PayerType`, `Customer`), no `save`/`orderRedirecting` request example ever captured.
- `checkRedirectPossible`'s whole contract (`types.ts:179,186-205`) — 6 of 20 response fields appear in no quote at all; the only redirect-check quote is the *edit* variant's request, covering the wrong direction.
- `updateRedirect`'s 15-field subset (`types.ts:268-282`) — circularly cited ("the docs example otherwise matches this module's already-documented field list"); masks a real naming conflict (`createRedirect` sends `RecipientSettlement`, `updateRedirect` sends `SettlementRecipient`+`CityRecipient` for the same concept, neither quoted).
- `getChangeEWOrdersList` (`types.ts:356-365`) — `spec.md:34` claims "confirmed verbatim" over an admitted source disagreement, with no quote behind the claim at all.
- `getReturnOrdersList` (req+11 resp fields), `getReturnReasons`, `getReturnReasonsSubtypes` — fully unsourced (`types.ts:139-173`).
- `deleteAdditionalServiceOrder`'s request/response schema — only the Ukrainian status-gate sentence is quoted, not the schema (`types.ts:375,379`).
- All three `save` results (`{Number, Ref}` for return/redirect/waybill-edit) — unsourced.
- 8 of 19 wire `calledMethod` literals never appear inside a quoted envelope, including a casing asymmetry (`checkPossibilityForRedirecting` lower-camel vs `CheckPossibilityCreateReturn`/`CheckPossibilityChangeEW` upper-camel) that Nova Poshta's case-sensitive API would reject if wrong — untestable by mock-echo tests.
- `checkReturnPossible.Number` (the field `createReturnIfPossible` depends on, AC-20) — unsourced, with an unquoted naming asymmetry (`Number` here vs `IntDocNumber` elsewhere).
- `PaymentMethod`'s `"NonCash"` member — sourced only via `internet-document`'s generated contract, which CLAUDE.md names explicitly as an invalid source ("a prior module's contract").
- `OnlyGetPricing` — two defects: value mismatch (`"1"` string vs the one quote's `true`) *and* the quote is from `update`, never `save`, so its presence on `save` at all is unsourced.
- Cost fields (`DeliveryCost`, `Cost`) typed `string` against the one quoted cost shown as a JSON number.
- `WaybillEditPossibility`'s 11 `Can…` flags typed strict `boolean` with no quote — the same defect class `NonCash` already shipped wrong once; the repo's own precedent (`tracking-document`) types wire-variable possibility flags `boolean | string`.
- `contracts/api-sync-report.md` itself grades ~40 rows `high` confidence while its own stated rubric (`:13-15`) requires a docs quote for that grade — several rows contradict the report's own definition (e.g. row 61 says "(2-SDK-confirmed subset)" in the origin cell, which by the rubric is `medium`, graded `high` anyway). This misgrading is why prior rounds' review missed most of the above.

### Stage-1 trace — additional findings beyond sourcing

- **AC-04 not actually enforced** — the mutually-exclusive return-destination-variant guarantee (spec.md, AC-04) only holds for fresh object literals, not a payload assembled field-by-field in a variable; verified with `tsc --noEmit --strict`: a mixed-variant variable produces zero errors when passed to `createReturn`/`calculateReturn`. The existing test (`test/unit/modules/additional-service.test.ts:331-343`) masks this with `as CreateReturnPayload`. **Fix:** add `?: never` cross-guards to each variant, drop the masking cast, add a real un-annotated-variable negative test.
- `spec.md` §7's sourcing KPI ("100% … confirmed against official docs") is not honestly reflected by §8's "substantially resolved, one narrow item remains" framing, given the scale above.
- `contracts/public-api.md` overstates ADR-0002's scope — claims `internet-document`'s `save`/`update` were refactored onto `client.requestFirst()`; in fact only `getDocumentPrice`/`getDocumentDeliveryDate` were. `save`/`update` still use the module-local `firstOrUndefined` (intentionally — different semantics), so only the contract doc is wrong.
- The promised opt-in integration suite for `additional-service` (spec.md §6, sad.md §8, tied to AC-05/AC-11) has no task and no file — `test/integration/` covers every other module but not this one.
- `sad.md` staleness is broader than rounds 1–2 swept: 8 sites total (§1 quality-goal-3, §1 ¶4's "proceeding past a blocking question" override, §4 decision 6's "remain genuinely open"/`Record<string,unknown>` claim, §5's edit-check `info` asymmetry note, Flow 1's and Flow 2's mermaid annotations, §10 QG-3, and the §12 glossary's non-optional `info`) — a superset of what round 2 already asked to fix (and which is still unfixed, since no `implement` ran between rounds 2 and 3).

### Stage-2 findings

- Full-replace semantics for `update` are asserted two contradictory ways (`types.ts:118` "not confirmed either way" vs `spec.md` §3 "confirmed… an omitted field is not carried forward") with no caller-facing JSDoc warning on `updateReturn`/`updateRedirect`, unlike `internet-document`'s precedent for the same hazard.
- Stale `NonCash` JSDoc comment still says "raw wire string" against the shipped `boolean` type (`src/modules/additional-service/index.ts:36-37`) — round 2 asked to fix this, still open (no `implement` ran yet).
- Test gaps: no module-level test for `success:true, data:[]` (the most plausible real "not possible" response for 12 `requestFirst()`-backed methods, and the one shape a wrong `OnlyGetPricing` couldn't otherwise surface through); `createReturnIfPossible`'s check-succeeds-then-create-declines path is uncovered; the 5 read methods' error-contract test asserts only the error *class*, not message propagation; no test pins the bare-object (non-array) `info` case (round 2 also asked for this, still open).

### Resolution (with the user)

- **Sourcing gap (all stage-1 sourcing findings above)** — **re-capture the full official docs
  page.** You will revisit `developers.novaposhta.ua`'s `AdditionalServiceGeneral` page in your own
  browser and paste its complete content (every method section: return, redirect, changeEW, lists,
  delete, reasons) so `implement` can quote every field genuinely, rather than the prior partial
  excerpt-only capture. Where the docs page itself doesn't cover a method, fall back to both SDKs
  with their structs actually pasted (not named).
- **AC-04 type-safety gap** — **Fix now** — add mutually-exclusive `?: never` guards, remove the
  masking cast, add a real negative test.
- **Stage-2 bundle (sad.md staleness sweep, NonCash JSDoc, update full-replace JSDoc warning, the
  4 test gaps)** — **Fix now** — bundle into the same `implement` pass.

## Gate result (round 3)

**CHANGES REQUESTED** — significantly larger scope than rounds 1–2 indicated: the API-contract
sourcing gap spans most of the 19-method surface, not the handful previously flagged, plus one
genuine stage-1 type-safety gap (AC-04) independent of sourcing. Nothing new regressed; 405 tests
still pass, lint and `tsc` still clean at the round-2 HEAD (`7e83717`) this round was run against.
All items resolved with the user (sourcing: re-capture docs; AC-04: fix now; stage-2 bundle: fix
now). **Blocking prerequisite:** the sourcing fixes cannot be implemented until the user supplies
the full docs-page capture — `implement` should request it before writing the sourcing-dependent
type/spec changes, and can proceed independently on AC-04 and the stage-2 bundle in the meantime.
